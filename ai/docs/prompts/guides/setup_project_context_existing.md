이 프로젝트는 기존 코드와 규칙이 있는 프로젝트다.

해야 할 일:
- README, AGENTS/CLAUDE 계열 문서, manifest, 주요 설정 파일, 검증 명령을 먼저 읽는다
- 파일만으로 확정 가능한 내용은 바로 `PROJECT_CONTEXT.md`에 채운다
- 파일만으로 확정할 수 없는 것만 마지막에 짧게 질문한다
- 마지막에 완성된 `PROJECT_CONTEXT.md` 초안을 보여준다

중요:
- 추측하지 말 것
- 기존 규칙과 충돌하면 충돌 지점도 표시할 것
