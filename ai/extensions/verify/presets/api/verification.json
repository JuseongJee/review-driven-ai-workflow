{
  "verifiers": {
    "k6": {
      "run": "k6 run --out json=$OUTPUT_DIR/k6-result.json $K6_SCRIPT",
      "criteria": [
        {
          "name": "latency",
          "description": "P95 응답 시간이 허용 기준 이내인가. TODO: set P95 threshold (e.g. p95<500ms).",
          "weight": "high"
        },
        {
          "name": "error-rate",
          "description": "에러율이 허용 기준 이하인가. TODO: set max error % (e.g. rate<0.01).",
          "weight": "high"
        }
      ],
      "evaluate": "위 criteria를 기준으로 각 항목을 1-5점으로 채점하고, 3점 미만인 항목의 구체적 개선사항을 제시하라.",
      "adapter": "ai/extensions/verify/verifiers/k6.sh"
    },
    "schema-validate": {
      "run": "node $SCHEMA_VALIDATE_SCRIPT --output $OUTPUT_DIR/schema-result.json",
      "criteria": [
        {
          "name": "response-schema",
          "description": "API 응답이 정의된 JSON Schema와 일치하는가.",
          "weight": "high"
        },
        {
          "name": "error-handling",
          "description": "오류 응답(4xx/5xx)이 표준 에러 포맷을 따르는가.",
          "weight": "medium"
        }
      ],
      "evaluate": "위 criteria를 기준으로 각 항목을 1-5점으로 채점하고, 3점 미만인 항목의 구체적 개선사항을 제시하라.",
      "adapter": "ai/extensions/verify/verifiers/schema-validate.sh"
    }
  }
}
